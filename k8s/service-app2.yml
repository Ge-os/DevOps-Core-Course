apiVersion: v1
kind: Service
metadata:
  name: devops-python-app-v2-service
  labels:
    app: devops-python-app-v2
    component: service
spec:
  type: ClusterIP
  selector:
    app: devops-python-app-v2
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
